# MEMORY — حافظهٔ پروژهٔ TamizChat

این فایل حافظهٔ بین‌چتی است. در شروع هر گفتگوی جدید اول این فایل را بخوان،
و در پایان هر کار «وضعیت فعلی» و «تصمیم‌ها» را به‌روزرسانی کن.

آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۲۱ (2026-08-12) — **بک‌اند کامل شد** (فاز ۱۱)

---

## پروژه چیست

TamizChat یک کلاینت ارتباطی self-hosted است؛ ترکیبی از قابلیت‌های Discord و
Google Meet با فلسفهٔ TeamSpeak: هر کسی بک‌اند را روی سرور خودش اجرا و کانفیگ
می‌کند و کاربر در اپ، آدرس سرور و پورت را وارد کرده و وصل می‌شود.

- بک‌اند: **Go** — پوشهٔ `backend/` (مخزن git همین‌جاست)
- فرانت: **C# / WinUI 3** — بعد از اتمام کامل بک‌اند شروع می‌شود

## قابلیت‌های کل پروژه

بر عهدهٔ بک‌اند: چت متنی، ویس چت، ویدیو چت، ارسال فایل و عکس، اسکرین‌شیر،
بات‌ها، paint board، سیستم ادمینی سبک TeamSpeak، رول‌های کاستوم، استیکر.

بر عهدهٔ فرانت (بک‌اند دخالتی ندارد): sound effect داخلی و کاستوم‌سازی آن،
voice changer، پخش صدای مخصوص هنگام kick، پخش صدای مخصوص هنگام ban،
تنظیمات میکروفون و شنیدن، bind کردن کلید برای شورتکات‌ها.

## تصمیم‌های قطعی معماری

| موضوع | تصمیم |
|-------|-------|
| زبان بک‌اند | Go (فعلاً go1.26) |
| مدیا (ویس/ویدیو/اسکرین‌شیر) | **LiveKit به‌عنوان سرویس جدا**؛ بک‌اند فقط توکن صادر و روم‌ها را مدیریت می‌کند |
| سیگنالینگ و رویدادها | WebSocket با پروتکل JSON نسخه‌دار |
| ذخیره‌سازی دائمی | **SQLite بدون CGo** (`modernc.org/sqlite`) — یک فایل `.db` |
| کانفیگ | داخل SQLite، **نه** فایل `.env` |
| پنل مدیریت | پنل تعاملی کامندلاین به سبک **x-ui**: روی سرور دستور `tamizchat` را می‌زنی و منوی شماره‌ای باز می‌شود |
| احراز هویت | بدون auth جدی — هر کلاینت هنگام نصب یک **UUID** می‌سازد، امن نگه می‌دارد و همیشه می‌فرستد؛ بک‌اند کاربر را با همان UUID می‌شناسد و مدیریت می‌کند |
| ماندگاری محتوای روم | **موقت**؛ با خروج آخرین نفر، چت و فایل و عکس روم از حافظه پاک می‌شود |

## منطق‌های کلیدی که باید رعایت شوند

- روم‌ها را ادمین می‌سازد/ویرایش می‌کند/حذف می‌کند: نام، رمز، سطح دسترسی بر
  اساس رول، ظرفیت.
- محتوای روم فقط در حافظه است. پاک‌سازی با یک مهلت کوتاه (`rooms.purge_grace_sec`)
  انجام می‌شود تا قطعی لحظه‌ای باعث از دست رفتن چت نشود.
- بات‌ها: چند بات هم‌زمان، قابل افزودن/حذف از پنل. بات موسیقی با نام و فولدر
  آهنگ کانفیگ می‌شود، مثل یک کاربر عادی به سرور وصل می‌شود، ادمین آن را بین
  روم‌ها move می‌کند، و کلاینت کنار پروفایل/راست‌کلیک آن پنل کوچکی برای
  next/prev/stop نشان می‌دهد.

## وضعیت فعلی

**هر ۱۱ فاز بک‌اند تمام شدند و تست دارند** (۲۲۴ تست، همه سبز).
بک‌اند از نظر نقشهٔ راه کامل است؛ قدم بعدی پروژه فرانت‌اند WinUI 3 است.
جزئیات فازها در [docs/ROADMAP.md](docs/ROADMAP.md) و قرارداد سیم در
[docs/PROTOCOL.md](docs/PROTOCOL.md).

```
backend/
  cmd/tamizchat/main.go          دستور واحد: panel (پیش‌فرض) | run | version
  internal/app/app.go            سیم‌کشی و چرخهٔ عمر سرور
  internal/config/schema.go      رجیستری تایپ‌دار تنظیمات (منبع حقیقت پنل)
  internal/config/config.go      نمای زندهٔ کانفیگ + Set/Reset/Watch
  internal/storage/store.go      باز کردن SQLite با WAL
  internal/storage/migrations.go مهاجرت‌های forward-only (۰۰۰۱ تا ۰۰۰۸)
  internal/storage/settings.go   settings + server_uuid + NewUUID
  internal/storage/users.go      جدول users: اولین/آخرین حضور، تعداد بازدید
  internal/storage/rooms.go      CRUD تعریف دائمی روم‌ها
  internal/storage/roles.go      رول‌ها و انتساب آن‌ها به کاربر
  internal/storage/sanctions.go  بن/میوت + لاگ اقدامات مدیریتی
  internal/protocol/             قرارداد سیم: پاکت، انواع پیام، کدهای خطا
  internal/session/session.go    یک نشست + صف خروجی بدون بلاک‌شدن + roomID
  internal/session/manager.go    رجیستری نشست‌ها، broadcast، جایگزینی UUID تکراری
  internal/session/validate.go   اعتبارسنجی UUID و نام کاربری
  internal/textutil/             نرمال‌سازی مشترک نام کاربر و نام روم
  internal/rooms/room.go         تعریف + اعضا + قلاب Ephemeral برای محتوای موقت
  internal/rooms/manager.go      CRUD روم، join/leave، انتقال عضویت، پاک‌سازی
  internal/chat/buffer.go        بافر پیام هر روم (پیاده‌سازی Ephemeral)
  internal/chat/manager.go       ارسال/ویرایش/حذف/تاریخچه/تایپ + نرخ
  internal/ratelimit/            token bucket عمومی
  internal/files/manager.go      تیکت آپلود، سهمیه، لینک دانلود
  internal/files/files.go        فایل‌های هر روم (پیاده‌سازی Ephemeral)
  internal/files/thumbnail.go    تصویر بندانگشتی بدون وابستگی بیرونی
  internal/media/token.go        امضای JWT لایوکیت + گرنت‌ها
  internal/media/client.go       کلاینت Twirp برای RoomService لایوکیت
  internal/media/manager.go      صدور توکن و اعمال مدیریت روی مدیا
  internal/paint/board.go        تختهٔ هر روم (پیاده‌سازی Ephemeral)
  internal/paint/manager.go      begin/append/end، undo، clear، نرخ
  internal/media/ingress.go      Ingress لایوکیت + تأیید امضای وب‌هوک
  internal/bots/library.go       اسکن فولدر موسیقی
  internal/bots/manager.go       صف پخش، کنترل، هماهنگی با Ingress
  internal/authz/                بیت‌مسک مجوزها + اینترفیس Policy
  internal/access/               رول‌ها، مجوزها، بن و میوت — همه در حافظه
  internal/gateway/gateway.go    upgrade + handshake
  internal/gateway/pump.go       read/write pump، dispatch، keepalive
  internal/gateway/rooms.go      هندلرهای room.* و نگاشت خطاها
  internal/gateway/chat.go       هندلرهای chat.* و نگاشت خطاها
  internal/gateway/admin.go      کیک/بن/میوت/موو + پخش رویدادهای مدیریتی
  internal/gateway/roles.go      CRUD رول و انتساب، محاسبهٔ مجوزهای کاربر
  internal/gateway/files.go      تیکت آپلود و لینک دانلود روی سوکت
  internal/gateway/media.go      توکن مدیا و وضعیت میکروفون/دوربین
  internal/gateway/paint.go      هندلرهای paint.* و نگاشت خطاها
  internal/gateway/bots.go       هندلرهای bot.* و نگاشت خطاها
  internal/httpapi/bots.go       فایل موسیقی برای Ingress + اندپوینت وب‌هوک
  internal/app/webhook.go        مسیریابی وب‌هوک لایوکیت به بات‌ها
  internal/panel/bots.go         ساخت و ویرایش بات در پنل
  internal/control/             سوکت کنترلی بین پنل و سرور در حال اجرا
  internal/app/control.go       پیاده‌سازی دستورهای کنترلی
  internal/rooms/reload.go      همگام‌سازی زندهٔ تعریف روم‌ها
  internal/panel/live.go        منوی کنترل سرور در حال اجرا
  internal/panel/service.go     ساخت فایل سرویس systemd
  internal/guard/               سقف اتصال و نرخ اتصال هر آی‌پی
  internal/httpapi/clientip.go  آی‌پی واقعی پشت reverse proxy
  internal/storage/backup.go    بکاپ با VACUUM INTO و بازیابی امن
  internal/panel/backup.go      بکاپ و بازیابی در پنل
  Makefile                      بیلد نسخه‌دار و چندسکویی
  docs/DEPLOY.md                راهنمای کامل اپراتور
  internal/httpapi/files.go      POST /api/v1/upload و GET /api/v1/file/{id}
  internal/panel/access.go       رول‌ها، بن‌ها و لاگ در پنل
  internal/httpapi/              /healthz و /api/v1/server-info و /ws
  internal/logging/              slog با تغییر سطح در زمان اجرا
  internal/version/              نسخه و کامیت (قابل تزریق با ldflags)
  internal/app/smoke_test.go     تست انتها به انتها روی سرور واقعی
  docs/ROADMAP.md                فازبندی کامل
  docs/PROTOCOL.md               مستند پروتکل برای کلاینت WinUI
```

### قواعد مهمی که در فاز ۲ تثبیت شد

- یک `client_uuid` = یک حضور. اتصال جدید با همان UUID، نشست قبلی را با دلیل
  `replaced_by_new_connection` می‌بندد و **هیچ** رویداد join/leave برای بقیه
  منتشر نمی‌شود (تا قطعی لحظه‌ای کاربر برای دیگران دیده نشود).
- کلاینت کند (صف خروجی پر) قطع می‌شود، نه اینکه حافظهٔ سرور را بزرگ کند.
- نام کاربری: کاراکتر کنترلی و کاراکترهای جهت‌دهی متن ممنوع (ضد جعل هویت)،
  یکتایی بدون حساسیت به حروف بزرگ/کوچک و فقط بین کاربران آنلاین.
- تمام نوشتن روی سوکت فقط از writePump انجام می‌شود (WebSocket تک‌نویسنده است).

### قواعد مهمی که در فاز ۳ تثبیت شد

- تعریف روم دائمی است، محتوای روم موقت. نقطهٔ اتصال `rooms.Ephemeral` همان
  جایی است که فاز ۴ (پیام‌ها)، فاز ۶ (فایل‌ها) و فاز ۸ (نقاشی) خودشان را
  ثبت می‌کنند؛ منطق پاک‌سازی هرگز لازم نیست بداند چه چیزی را پاک می‌کند.
- `room.member_joined/left` **سرورگستر** پخش می‌شوند، نه فقط داخل روم — چون
  کلاینت مثل TeamSpeak کل درخت روم‌ها را نشان می‌دهد. به همین دلیل هنگام
  جابه‌جایی روم، `user.updated` جداگانه فرستاده نمی‌شود (یک رویداد برای یک
  اتفاق). `user.updated` فقط برای تغییر نام است.
- اکشن‌های مدیریتی روم شناسهٔ انجام‌دهنده را می‌گیرند تا او از broadcast
  عمومی مستثنا شود و فریم تکراری نگیرد (پاسخ خودش با `id` همبسته است).
- هر تغییر مجوز باید از `internal/authz` بگذرد؛ الان `OpenPolicy` است و
  همه‌چیز را اجازه می‌دهد. **تا فاز ۵ سرور را بدون رمز سرور بالا نیاورید.**
- میان‌افزار HTTP باید `Unwrap`/`Hijack` داشته باشد وگرنه ارتقای WebSocket
  با خطای ۵۰۱ می‌شکند (این باگ واقعاً رخ داد و تست رگرسیون دارد).

### قواعد مهمی که در فاز ۴ تثبیت شد

- بافر چت هر روم خودش را به‌عنوان `rooms.Ephemeral` ثبت می‌کند. هیچ کد
  جدیدی برای پاک‌سازی نوشته نشد؛ همان مسیر فاز ۳ کار می‌کند. فاز ۶ و ۸ هم
  باید دقیقاً همین کار را بکنند.
- **ویرایش فقط برای نویسنده، حتی برای مدیر.** مدیر می‌تواند پیام دیگران را
  حذف کند ولی نه ویرایش — وگرنه می‌شود حرف در دهان کسی گذاشت. این تفکیک در
  `authz.Policy.CanModerateChat` و در تست‌ها تثبیت شده.
- پیامی که سرور رد می‌کند از سهمیهٔ نرخ کم نمی‌شود؛ وگرنه کسی که پیام بلند
  می‌نویسد به‌جرم اسپم ساکت می‌شود.
- پیام حذف‌شده کامل حذف می‌شود (بدون tombstone) — با روح موقتی‌بودن روم جور
  است.
- `seq` فقط داخل یک روم زنده معنا دارد و با پاک‌سازی روم ادامه می‌یابد (ریست
  نمی‌شود) تا کلاینت‌های قدیمی گیج نشوند.
- `rooms.Manager.OnDelete` نقطهٔ آزادسازی حافظهٔ per-room است؛ فاز ۶ هم باید
  از آن استفاده کند.
- **تله در تست‌ها:** هر کلاینت تستی باید همهٔ فریم‌های پخش‌شده را مصرف کند،
  وگرنه `expect` بعدی فریم قبلی را می‌بیند. برای فریم‌هایی که ترتیبشان بخشی
  از قرارداد نیست از `expectFrames` استفاده کنید. فیکسچر تست نرخ را عمداً
  بالا می‌برد؛ تست‌های نرخ خودشان پایینش می‌آورند.

### محدودیت‌های شناخته‌شدهٔ فعلی

- پنل مستقیم روی فایل دیتابیس می‌نویسد، پس تغییرات روی سرورِ **در حال اجرا**
  تا فاز ۱۰ فقط بعد از ری‌استارت اعمال می‌شوند.
- پنل فقط کاربران ذخیره‌شده را نشان می‌دهد؛ لیست آنلاین‌ها در حافظهٔ پروسهٔ
  سرور است و به سوکت کنترلی فاز ۱۰ نیاز دارد.
- روی این ویندوز `go test -race` کار نمی‌کند چون gcc نصب نیست؛ تست‌ها بدون
  race detector اجرا شده‌اند.
- اکشن‌های مدیریتی (کیک/بن/میوت/موو) فقط روی کاربر **آنلاین** کار می‌کنند؛
  بن‌کردن کاربر آفلاین از پنل انجام می‌شود. برداشتن بن روی آفلاین هم کار می‌کند.
- تغییر رول‌ها روی نشست‌های آنلاین با `refreshRoles` اعمال می‌شود، ولی
  تغییرات پنل تا ری‌استارت سرور دیده نمی‌شوند (همان محدودیت فاز ۱۰).
- در چت: پاسخ به پیام (reply) و پیام سیستمی روم هنوز نیستند.
- بستهٔ استیکر میزبانی‌شده روی سرور هنوز نیست: زیرساخت فایل حالا هست، ولی
  استیکرها برخلاف فایل‌های روم باید **دائمی** باشند و مسیر جدا می‌خواهند.
- ~~تغییرات پنل نیاز به ری‌استارت دارند~~ — در فاز ۱۰ حل شد.
- start/stop/restart مستقیم از پنل نیست: روی لینوکس کار systemd است و پنل
  فایل سرویسش را می‌سازد.

### قواعد مهمی که در فاز ۵ تثبیت شد

- **قاعدهٔ رتبه (priority):** فقط روی کسی می‌شود اقدام کرد که رتبه‌اش اکیداً
  کمتر باشد. دو ادمین هم‌رتبه نمی‌توانند همدیگر را کیک/بن/میوت کنند. همچنین
  نمی‌شود رولی هم‌رتبه یا بالاتر از خود ساخت یا داد، و نمی‌شود مجوزی به رول
  داد که خودت نداری. این دو، مسیرهای ارتقای خودسرانه را می‌بندند.
- مجوزها روی سیم **کلید متنی** هستند نه عدد، تا کلاینت قدیمی‌تر هم آن‌هایی را
  که می‌شناسد بفهمد. بیت‌ها هرگز جابه‌جا نشوند (در دیتابیس ذخیره می‌شوند).
- کل وضعیت دسترسی در حافظه است چون در هر پیام بررسی می‌شود.
- `access.Manager` هم `authz.Policy` است، هم `rooms.Access`، هم
  `chat.Sanctions` — یک منبع حقیقت.
- **بستن اتصال از سمت سرور با `CloseNow` انجام می‌شود، نه بستن محترمانه.**
  بستن محترمانه منتظر پاسخ کلاینت می‌ماند و کسی که کیک شده دیگر نمی‌خواند؛
  نتیجه‌اش نشستِ زنده تا چند ثانیه بعد از اخراج بود (این باگ واقعاً رخ داد و
  تست دارد). دلیل، قبلش به‌صورت فریم برنامه‌ای فرستاده می‌شود.
- `admin.move` عمداً رمز، رول و ظرفیت روم را نادیده می‌گیرد.
- اولین ادمین سرور فقط از پنل ساخته می‌شود؛ پنل تابع قاعدهٔ رتبه نیست چون
  کسی که به آن دسترسی دارد صاحب سرور است.

### قواعد مهمی که در فاز ۶ تثبیت شد

- **مجوز از سوکت، بایت‌ها از HTTP.** کلاینت اول `file.upload_request`
  می‌فرستد و تیکت می‌گیرد؛ سرور پیش از دریافت حتی یک بایت، مجوز و حجم و
  سهمیه را بررسی می‌کند. تیکت یک‌بارمصرف است. این الگو برای هر انتقال حجیم
  بعدی هم باید تکرار شود.
- نوع فایل از **خود بایت‌ها** تشخیص داده می‌شود، نه از پسوند یا ادعای کلاینت.
- دانلود همیشه `attachment` + `nosniff` است تا فایل آپلودشده نتواند روی
  دامنهٔ سرور اجرا شود. تنها استثنا، تصویر بندانگشتی است که همیشه JPEG است.
- لینک دانلود به رومی که کاربر **همین حالا** داخلش است محدود می‌شود؛ شناسهٔ
  درزکرده از روم دیگر کار نمی‌کند.
- فایل روی دیسک با شناسهٔ تصادفی ذخیره می‌شود؛ نام کاربر فقط برای نمایش است.
- فایل‌ها هم مثل چت `rooms.Ephemeral` هستند و با پاک‌سازی روم از **دیسک**
  حذف می‌شوند. کل پوشهٔ آپلود در استارتاپ پاک می‌شود.
- کوچک‌سازی تصویر با میانگین‌گیری سطحی نوشته شد نه nearest-neighbour، چون
  nearest روی عکس واقعی نتیجهٔ دانه‌دانه و خراب می‌دهد. بدون وابستگی جدید.

### قواعد مهمی که در فاز ۷ تثبیت شد

- **SDK لایوکیت اضافه نشد.** توکن یک JWT/HS256 ساده است و RoomService هم
  Twirp روی HTTP+JSON؛ هر دو مستقیم پیاده شدند. SDK یک استک کامل WebRTC را
  به سروری می‌آورد که هرگز بستهٔ RTP لمس نمی‌کند.
- گرنت توکن باید مجوزها را منعکس کند (`canPublishSources`). پرچم‌های `can_*`
  فقط برای رابط کاربری‌اند؛ اعمال واقعی سمت LiveKit است.
- **بیت‌های مجوز قفل شده‌اند** با تست `TestPermissionBitsAreStable`. هر مجوز
  جدید = یک بیت جدید در انتها + یک مهاجرت که آن را به رول‌های موجود می‌دهد
  (الگویش مهاجرت ۰۰۰۶ است). جابه‌جا کردن بیت‌ها یعنی رول‌های موجود معنی
  عوض می‌کنند.
- تمام تماس‌های LiveKit **best-effort با timeout** هستند و در goroutine جدا
  اجرا می‌شوند. خرابی یا نبود LiveKit نباید چت را زمین بزند.
- میوت باید به LiveKit برسد (`UpdateParticipant`)، وگرنه کاربر میوت‌شده در
  چت ساکت است ولی روی صدا همچنان حرف می‌زند.
- وضعیت «در حال صحبت» عمداً سمت سرور نگه داشته نمی‌شود — LiveKit خودش رویداد
  active speaker را به کلاینت می‌دهد. فقط mic/cam/screen که خود کلاینت گزارش
  می‌دهد نگه داشته می‌شود، و با خروج از روم ریست می‌شود.
- `rooms.Manager.OnMemberLeft` قلاب مرکزی خروج از روم است (خروج عادی، تعویض
  روم، جابه‌جایی توسط ادمین، قطع اتصال). فازهای بعدی هم از همین استفاده کنند.

### قواعد مهمی که در فاز ۸ تثبیت شد

- **خط‌ها جریانی‌اند** (begin/append/end). نقاشی باید همان‌طور که کشیده
  می‌شود دیده شود؛ فرستادن خط کامل بعد از برداشتن ماوس حس محصول را خراب
  می‌کند.
- `paint.append` عمداً **پاسخ ندارد** — پرتکرارترین پیام کل سیستم است و
  کلاینت نقطه را محلی کشیده. فقط خطاها فریم می‌گیرند.
- مختصات **نرمال‌شده ۰..۱** هستند، نه پیکسل، وگرنه نقاشی روی رزولوشن دیگر
  جابه‌جا می‌افتد. سرور NaN و بی‌نهایت را دور می‌ریزد — یک کلاینت خراب نباید
  رندر بقیه را به هم بریزد.
- تختهٔ پر، خط جدید را **رد می‌کند** و قدیمی‌ترها را دور نمی‌ریزد. برخلاف چت
  (که بافر حلقوی دارد)، پاک‌کردن نامحسوس ابتدای یک نقاشی بدتر از خطا دادن است.
- `clear` با دامنهٔ `all` مجوز `moderate_chat` می‌خواهد چون کار دیگران را
  نابود می‌کند؛ `mine` برای همه آزاد است.
- کاربر میوت‌شده نمی‌تواند نقاشی کند (همان منطق: خط‌خطی‌کردن = داد زدن).
- الگوی «مجوز جدید = بیت جدید + مهاجرت» دوباره اجرا شد (مهاجرت ۰۰۰۷). این
  دیگر روال ثابت است.

### قواعد مهمی که در فاز ۹ تثبیت شد

- **مسیر صدای بات: LiveKit Ingress** (انتخاب کاربر). بک‌اند هیچ بایت صوتی را
  لمس نمی‌کند؛ Ingress فایل را از یک اندپوینت HTTP همین سرور می‌گیرد. یعنی
  SDK لایوکیت و pion هنوز به پروژه اضافه نشده‌اند.
- **الزام تازه برای اپراتور:** `network.public_host` باید مقدار داشته باشد،
  وگرنه Ingress نمی‌داند فایل را از کجا بگیرد. سرور نمی‌تواند حدسش بزند و
  خطای صریح با راهنمایی می‌دهد.
- **pause عمداً نیست** — با Ingress، ادامه یعنی شروع از اول آهنگ. دکمه‌ای که
  وسط آهنگ را می‌پراند بدتر از نبودنش است. اگر روزی مسیر صدا عوض شد
  (server-sdk-go)، آن‌وقت pause واقعی ممکن می‌شود.
- **کنترل صدا سمت سرور نیست** — هر شنونده در کلاینت خودش تنظیم می‌کند.
- پایان آهنگ فقط از **وب‌هوک لایوکیت** فهمیده می‌شود. امضا (JWT با ادعای
  `sha256` برابر هش بدنه) قبل از هر اقدامی بررسی می‌شود؛ این اندپوینت احراز
  هویت دیگری ندارد.
- هویت بات در لایوکیت `bot-<id>` است تا هرگز با `client_uuid` اشتباه نشود.
- **تله در تست‌ها:** صف بر اساس **نام فایل** مرتب می‌شود. تستی که فایل‌ها را
  `one/two/three` نام‌گذاری کند به دام ترتیب الفبایی می‌افتد؛ از نام‌های
  شماره‌دار استفاده کنید.
- الگوی «انجام‌دهنده از broadcast مستثنا، پاسخ خودش با `id` همبسته» دوباره
  اجرا شد (این بار برای بات‌ها). این دیگر قانون ثابت پروژه است.

### قواعد مهمی که در فاز ۱۰ تثبیت شد

- **بدهی «نیاز به ری‌استارت» صاف شد.** پنل همچنان اول در دیتابیس می‌نویسد،
  بعد به سرور در حال اجرا می‌گوید `reload`. هر جزء تازه‌ای که وضعیت را در
  حافظه نگه می‌دارد، باید یک `Reload` هم داشته باشد و به
  `controlHandler.Reload` وصل شود.
- کانال کنترلی: TCP روی **loopback با پورت تصادفی** + توکن تازه در هر اجرا،
  در `control.json` کنار دیتابیس با دسترسی ۰۶۰۰. مرز اعتماد همان مرز
  دیتابیس است: هرکس بتواند پوشهٔ داده را بخواند، از قبل همه‌کاره است.
- توکن با `subtle.ConstantTimeCompare` مقایسه می‌شود، حتی روی loopback.
- فایل endpoint موقع خاموش‌شدن تمیز حذف می‌شود؛ ولی kill سخت آن را جا
  می‌گذارد، پس کلاینت **حتماً باید dial کند** نه اینکه به وجود فایل اعتماد
  کند (تست دارد).
- `network.listen_addr` تنها تنظیمی است که reload اعمالش نمی‌کند — listener
  یک‌بار bind می‌شود. پنل همان‌جا صریح می‌گوید.
- reload روم‌ها عضویت را دست نمی‌زند، مگر رومی که حذف شده باشد؛ آنجا اعضا
  با دلیل `room_deleted` بیرون می‌آیند.
- در پکیج panel اسم `ok` برای تابع کمکی ممنوع شد (با `v, ok :=` تداخل
  می‌کرد)؛ حالا `green` است.

### قواعد مهمی که در فاز ۱۱ تثبیت شد

- **رد کردن قبل از upgrade.** سقف اتصال در `ServeHTTP` قبل از
  `websocket.Accept` بررسی می‌شود، پس سیل اتصال یک پاسخ HTTP هزینه دارد نه
  یک WebSocket کامل.
- پشت reverse proxy، `network.trusted_proxies` **حتماً** باید ست شود، وگرنه
  همهٔ اتصال‌ها از `127.0.0.1` دیده می‌شوند و سقف هر آی‌پی بی‌معنا می‌شود.
  و بدون آن، باور کردن X-Forwarded-For یعنی هرکس می‌تواند آدرس جعل کند.
  زنجیرهٔ XFF از **راست به چپ** خوانده می‌شود و هاپ‌های مورد اعتماد رد
  می‌شوند.
- panic در هندلر یک فریم، فقط همان فریم را می‌بازد. در recover حتماً
  type assertion را checked بنویسید (`err, ok := rec.(error)`) وگرنه panic
  با رشته، داخل خود recover دوباره panic می‌کند.
- بکاپ با `VACUUM INTO` گرفته می‌شود نه کپی فایل — کپی ساده از دیتابیس در
  حال نوشتن می‌تواند صفحهٔ نیمه‌نوشته بگیرد یا WAL را جا بگذارد.
- بازیابی، دیتابیس فعلی را **کنار می‌گذارد** نه حذف؛ و فایل‌های `-wal`/`-shm`
  قدیمی باید پاک شوند وگرنه دیتابیس بازیابی‌شده خراب می‌شود.
- `WriteTimeout` روی http.Server ست **نشده** و نباید بشود: سوکت‌ها طولانی‌مدت
  باز می‌مانند و آن تایم‌اوت WebSocket را می‌کشد.

## وضعیت نهایی بک‌اند

نقشهٔ راه تمام شد. کارهای عمداً موکول‌شده در انتهای `docs/ROADMAP.md` فهرست
شده‌اند (reply، استیکر سروری، pause واقعی بات، وب‌هوک‌های بیشتر، race در CI).

## قدم بعدی

**فرانت‌اند: C# و WinUI 3.** قرارداد کامل سیم در `docs/PROTOCOL.md` است و
همه‌چیزی که کلاینت لازم دارد آنجاست: پاکت پیام، handshake، همهٔ انواع پیام،
کدهای خطای پایدار، و جریان‌های فایل/مدیا/نقاشی/بات.

## قواعد کار

- هر کار مربوط به بک‌اند فقط داخل `backend/` انجام می‌شود.
- مهاجرت‌های منتشرشده هرگز ویرایش نمی‌شوند؛ مهاجرت جدید اضافه می‌شود.
- هر تنظیم جدید باید در `internal/config/schema.go` ثبت شود تا خودکار در پنل
  ظاهر شود.
- بعد از اتمام هر فاز: جدول ROADMAP و بخش «وضعیت فعلی» همین فایل به‌روز شود.
