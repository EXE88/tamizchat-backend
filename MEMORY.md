# MEMORY — حافظهٔ پروژهٔ TamizChat

این فایل حافظهٔ بین‌چتی است. در شروع هر گفتگوی جدید اول این فایل را بخوان،
و در پایان هر کار «وضعیت فعلی» و «تصمیم‌ها» را به‌روزرسانی کن.

آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۱۹ (2026-08-10)

---

## پروژه چیست

TamizChat یک کلاینت ارتباطی self-hosted است؛ ترکیبی از قابلیت‌های Discord و
Google Meet با فلسفهٔ TeamSpeak: هر کسی بک‌اند را روی سرور خودش اجرا و کانفیگ
می‌کند و کاربر در اپ، آدرس سرور و پورت را وارد کرده و وصل می‌شود.

- بک‌اند: **Go** — پوشهٔ `backend/` (مخزن git همین‌جاست)
- فرانت: **C# / WinUI 3** — بعد از اتمام کامل بک‌اند شروع می‌شود

## قابلیت‌های کل پروژه

بر عهدهٔ بک‌اند: چت متنی، ویس چت، ویدیو چت، ارسال فایل و عکس، اسکرین‌شیر،
بات‌ها، paint board، سیستم ادمینی سبک TeamSpeak، رول‌های کاستوم، استیکر.

بر عهدهٔ فرانت (بک‌اند دخالتی ندارد): sound effect داخلی و کاستوم‌سازی آن،
voice changer، پخش صدای مخصوص هنگام kick، پخش صدای مخصوص هنگام ban،
تنظیمات میکروفون و شنیدن، bind کردن کلید برای شورتکات‌ها.

## تصمیم‌های قطعی معماری

| موضوع | تصمیم |
|-------|-------|
| زبان بک‌اند | Go (فعلاً go1.26) |
| مدیا (ویس/ویدیو/اسکرین‌شیر) | **LiveKit به‌عنوان سرویس جدا**؛ بک‌اند فقط توکن صادر و روم‌ها را مدیریت می‌کند |
| سیگنالینگ و رویدادها | WebSocket با پروتکل JSON نسخه‌دار |
| ذخیره‌سازی دائمی | **SQLite بدون CGo** (`modernc.org/sqlite`) — یک فایل `.db` |
| کانفیگ | داخل SQLite، **نه** فایل `.env` |
| پنل مدیریت | پنل تعاملی کامندلاین به سبک **x-ui**: روی سرور دستور `tamizchat` را می‌زنی و منوی شماره‌ای باز می‌شود |
| احراز هویت | بدون auth جدی — هر کلاینت هنگام نصب یک **UUID** می‌سازد، امن نگه می‌دارد و همیشه می‌فرستد؛ بک‌اند کاربر را با همان UUID می‌شناسد و مدیریت می‌کند |
| ماندگاری محتوای روم | **موقت**؛ با خروج آخرین نفر، چت و فایل و عکس روم از حافظه پاک می‌شود |

## منطق‌های کلیدی که باید رعایت شوند

- روم‌ها را ادمین می‌سازد/ویرایش می‌کند/حذف می‌کند: نام، رمز، سطح دسترسی بر
  اساس رول، ظرفیت.
- محتوای روم فقط در حافظه است. پاک‌سازی با یک مهلت کوتاه (`rooms.purge_grace_sec`)
  انجام می‌شود تا قطعی لحظه‌ای باعث از دست رفتن چت نشود.
- بات‌ها: چند بات هم‌زمان، قابل افزودن/حذف از پنل. بات موسیقی با نام و فولدر
  آهنگ کانفیگ می‌شود، مثل یک کاربر عادی به سرور وصل می‌شود، ادمین آن را بین
  روم‌ها move می‌کند، و کلاینت کنار پروفایل/راست‌کلیک آن پنل کوچکی برای
  next/prev/stop نشان می‌دهد.

## وضعیت فعلی

**فاز ۱ تمام شد و تست شد.** جزئیات فازها در [docs/ROADMAP.md](docs/ROADMAP.md).

آنچه ساخته شد:

```
backend/
  cmd/tamizchat/main.go          دستور واحد: panel (پیش‌فرض) | run | version
  internal/app/app.go            سیم‌کشی و چرخهٔ عمر سرور
  internal/config/schema.go      رجیستری تایپ‌دار تنظیمات (منبع حقیقت پنل)
  internal/config/config.go      نمای زندهٔ کانفیگ + Set/Reset/Watch
  internal/storage/store.go      باز کردن SQLite با WAL
  internal/storage/migrations.go مهاجرت‌های forward-only
  internal/storage/settings.go   settings + server_uuid + NewUUID
  internal/httpapi/              /healthz و /api/v1/server-info
  internal/logging/              slog با تغییر سطح در زمان اجرا
  internal/version/              نسخه و کامیت (قابل تزریق با ldflags)
  docs/ROADMAP.md                فازبندی کامل
```

نکتهٔ مهم (محدودیت شناخته‌شدهٔ فعلی): پنل مستقیم روی فایل دیتابیس می‌نویسد،
پس تغییرات تا **فاز ۱۰** روی سرورِ در حال اجرا فقط بعد از ری‌استارت اعمال
می‌شوند. در فاز ۱۰ یک سوکت کنترلی اضافه می‌شود.

## قدم بعدی

فاز ۲: هویت کاربر (UUID) و WebSocket gateway.

## قواعد کار

- هر کار مربوط به بک‌اند فقط داخل `backend/` انجام می‌شود.
- مهاجرت‌های منتشرشده هرگز ویرایش نمی‌شوند؛ مهاجرت جدید اضافه می‌شود.
- هر تنظیم جدید باید در `internal/config/schema.go` ثبت شود تا خودکار در پنل
  ظاهر شود.
- بعد از اتمام هر فاز: جدول ROADMAP و بخش «وضعیت فعلی» همین فایل به‌روز شود.
